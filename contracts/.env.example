# Amoy testnet RPC, e.g. from Alchemy or Infura
POLYGON_RPC_URL=
# Deployer wallet private key — funded with free testnet MATIC from a faucet.
# NEVER commit a real key. Can be the same wallet as the backend's signer
# (backend/.env's POLYGON_PRIVATE_KEY) for a demo — keep it testnet-only.
POLYGON_PRIVATE_KEY=
